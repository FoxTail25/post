import { createContext } from "react"
// import api from "../utils/api";


// const [userData, setUserData] = useState([]);
// useEffect(() => { api.getUserInfo().then((data) => setUserData(data)) }, [])
// let uData
// api.getUserInfo().then((data) => uData = data)

// let pData
// api.getAllPosts().then((data) => pData = data)
// const [postData, setPostData] = useState([]);
// useEffect(() => { api.getAllPosts().then((data) => setPostData(data)) }, [])


export const AllContextData = createContext();
